_schema-version: 3.3.0
ID: VehicleFleetMaintenance
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: VehicleFleetMaintenance-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: VehicleFleetMaintenance-auth
      - name: VehicleFleetMaintenance-db
      - name: VehicleFleetMaintenance-destination

  - name: VehicleFleetMaintenance-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: VehicleFleetMaintenance-db
  - name: VehicleFleetMaintenance
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: VehicleFleetMaintenance-auth
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

resources:
  - name: VehicleFleetMaintenance-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: VehicleFleetMaintenance-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'Driver (VehicleFleetMaintenance ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Driver'
          - name: 'FleetCoordinator (VehicleFleetMaintenance ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.FleetCoordinator'
          - name: 'FleetManager (VehicleFleetMaintenance ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.FleetManager'
          - name: 'Administrator (VehicleFleetMaintenance ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Administrator'
          - name: 'Admin (VehicleFleetMaintenance ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Admin'
    requires:
      - name: app-api
  - name: VehicleFleetMaintenance-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: VehicleFleetMaintenance-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
