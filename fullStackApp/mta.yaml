_schema-version: 3.3.0
ID: fullStackApp
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: fullStackApp-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: fullStackApp-db
      - name: fullStackApp-auth
      - name: fullStackApp-destination

  - name: fullStackApp-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: fullStackApp-db
  - name: fullStackApp
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: fullStackApp-auth
      - name: fullStackApp-destination
      - name: fullStackApp-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: fullStackApp-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: fullStackAppfullstackapp
          artifacts:
            - fullstackapp.zip
          target-path: app/
    requires:
      - name: fullStackApp-auth
      - name: srv-api
      - name: fullStackApp-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: fullStackAppfullstackapp
    type: html5
    path: app/fullstackapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: fullStackApp-destinations
    type: com.sap.application.content
    requires:
      - name: fullStackApp-auth
        parameters:
          service-key:
            name: fullStackApp-auth-key
      - name: fullStackApp-html5-repo-host
        parameters:
          service-key:
            name: fullStackApp-html5-repo-host-key
      - name: srv-api
      - name: fullStackApp-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: fullStackApp-html5-repository
              ServiceInstanceName: fullStackApp-html5-repo-host
              ServiceKeyName: fullStackApp-html5-repo-host-key
              sap.cloud.service: fullStackApp.service
            - Name: fullStackApp-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: fullStackApp-auth
              ServiceKeyName: fullStackApp-auth-key
              sap.cloud.service: fullStackApp.service

resources:
  - name: fullStackApp-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: fullStackApp-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: fullStackApp-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
    requires:
      - name: app-api
  - name: fullStackApp-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: fullStackApp-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: fullStackApp-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
